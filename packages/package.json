{
  "name": "@turbo-asset/packages",
  "version": "1.0.0",
  "description": "Turbo Asset NAPI-RS Package Collection",
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "build:all": "npm run build --workspaces",
    "test:all": "npm run test --workspaces",
    "clean:all": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "@types/node": "^20.19.12",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}